# 🚂 Развертывание бота на Railway

## 📦 Быстрый старт

### 1. Подготовка проекта

Все файлы уже готовы:
- ✅ `main.py` - основной файл бота
- ✅ `requirements.txt` - зависимости
- ✅ `Procfile` - команда запуска
- ✅ `railway.json` - конфигурация Railway

### 2. Регистрация на Railway

1. Перейдите на [railway.app](https://railway.app)
2. Зарегистрируйтесь через GitHub (рекомендуется)
3. Нажмите **"New Project"**

### 3. Развертывание

#### Вариант А: Из GitHub репозитория (Рекомендуется)

1. **Создайте репозиторий на GitHub** (если еще нет):
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin <your-repo-url>
   git push -u origin main
   ```

2. **В Railway:**
   - Выберите **"Deploy from GitHub repo"**
   - Выберите ваш репозиторий
   - Railway автоматически определит Python проект

3. **Настройте переменные окружения:**
   - Перейдите в раздел **Variables**
   - Добавьте (опционально, так как токен уже в коде):
     - `TELEGRAM_BOT_TOKEN` = `8324737437:AAFhXagyrNmX4aCtVha032GhR63Rl36TAi8`
     - `OWNER_ID` = `1716175980`
     - `ADMIN_IDS` = `1716175980,751886453`
     - `DB_FILE` = `nz_bot.db` (по умолчанию)

#### Вариант Б: Прямая загрузка

1. В Railway выберите **"Empty Project"**
2. Нажмите **"Add Service"** → **"GitHub Repo"** или **"Local Directory"**
3. Выберите ваш проект
4. Railway автоматически определит Python и установит зависимости

### 4. Настройка переменных окружения

**Важно:** Токен, админы и owner ID уже встроены в код, поэтому переменные окружения не обязательны!

Но если хотите использовать переменные (рекомендуется для безопасности):

1. Перейдите в ваш проект на Railway
2. Откройте вкладку **Variables**
3. Добавьте:
   ```
   TELEGRAM_BOT_TOKEN=8324737437:AAFhXagyrNmX4aCtVha032GhR63Rl36TAi8
   OWNER_ID=1716175980
   ADMIN_IDS=1716175980,751886453
   ```

### 5. Запуск

Railway автоматически:
- ✅ Определит Python проект
- ✅ Установит зависимости из `requirements.txt`
- ✅ Запустит команду из `Procfile` или `railway.json`

### 6. Проверка работы

1. Перейдите во вкладку **Deployments**
2. Дождитесь успешного деплоя (зеленая галочка)
3. Откройте вкладку **Logs** и проверьте вывод:
   ```
   ✅ База даних (SQLite) ініціалізована (з шифруванням)
   🚀 NZ.UA Telegram Bot запущено!
   📱 Бот готовий до роботи
   ```
4. Попробуйте отправить боту `/start` в Telegram

---

## 🔧 Решение проблем

### Проблема: Бот не запускается

**Решение:**
1. Проверьте логи в Railway (вкладка **Logs**)
2. Убедитесь, что все зависимости установлены
3. Проверьте, что токен правильный

### Проблема: "Module not found"

**Решение:** Railway автоматически устанавливает зависимости. Если проблема сохраняется:
- Проверьте `requirements.txt`
- Посмотрите логи сборки

### Проблема: Бот не отвечает

**Решение:**
1. Проверьте логи - там должны быть сообщения о запуске
2. Убедитесь, что деплой успешен (зеленая галочка)
3. Проверьте токен бота

### Проблема: База данных не сохраняется

**Решение:** SQLite файл сохраняется в файловой системе Railway. Но при пересоздании сервиса данные могут потеряться.

Для постоянного хранения рекомендуется:
- Использовать Railway Volume (платный план)
- Или использовать внешнюю БД (PostgreSQL)

---

## 📊 Структура файлов

```
nzdiary/
├── main.py              ✅ Основной файл бота
├── requirements.txt     ✅ Зависимости
├── Procfile            ✅ Команда запуска для Railway
├── railway.json        ✅ Конфигурация Railway
└── nz_bot.db          ⚡ Создастся автоматически
```

---

## 💰 Стоимость

**Railway Free Plan:**
- $5 бесплатных кредитов каждый месяц
- ~500 часов работы приложения (достаточно для бота)
- После исчерпания кредитов проект останавливается до следующего месяца

**Hobby Plan ($5/месяц):**
- $5 кредитов + неограниченные часы
- Подходит для постоянной работы бота

---

## 🔐 Безопасность

✅ **Токен уже встроен в код** - это нормально для быстрого старта.

⚠️ **Для продакшена рекомендуется:**
- Использовать переменные окружения вместо хардкода токена
- Не коммитить токены в публичные репозитории
- Использовать `.gitignore` для исключения чувствительных файлов

---

## ✅ Чек-лист

- [ ] Создан аккаунт на Railway
- [ ] Проект загружен (через GitHub или напрямую)
- [ ] Переменные окружения настроены (опционально)
- [ ] Деплой успешен (зеленая галочка)
- [ ] Бот отвечает в Telegram

---

## 🎉 Готово!

Бот должен работать на Railway! Если есть вопросы, проверьте логи в Railway или обратитесь за помощью.

---

## 📝 Дополнительная информация

Railway автоматически:
- Определяет язык проекта (Python)
- Устанавливает зависимости
- Запускает приложение
- Предоставляет HTTPS
- Управляет процессами

Не нужно настраивать веб-серверы, прокси или SSL сертификаты - все работает из коробки!

