# NZ Diary Bot

Telegram бот для роботи з системою NZ.ua - отримання оцінок, розкладу, новин та іншої інформації.

## 🚀 Налаштування

### Вимоги

- Python 3.8+
- Telegram Bot Token (отримайте у [@BotFather](https://t.me/BotFather))

### Встановлення

1. Клонуйте репозиторій:
```bash
git clone <your-repo-url>
cd nzdiary
```

2. Створіть віртуальне середовище:
```bash
python -m venv venv
```

3. Активуйте віртуальне середовище:
- Windows:
```bash
venv\Scripts\activate
```
- Linux/Mac:
```bash
source venv/bin/activate
```

4. Встановіть залежності:
```bash
pip install -r requirements.txt
```

5. Налаштуйте змінні середовища:
```bash
# Windows (PowerShell)
$env:TELEGRAM_BOT_TOKEN="your_bot_token_here"

# Linux/Mac
export TELEGRAM_BOT_TOKEN="your_bot_token_here"
```

Або створіть файл `.env` на основі `env.example` (не комітьте `.env` в репозиторій!):
```bash
cp env.example .env
# Потім відредагуйте .env та вставте свій токен
```

6. Запустіть бота:
```bash
python main.py
```

## 📋 Функції

- 🔐 Авторизація в системі NZ.ua
- 📊 Перегляд оцінок та середнього балу
- 📅 Розклад уроків
- 📰 Новинна стрічка
- ⭐ VIP функції (автоматичні перевірки, експорт даних)
- 🎯 Нагадування про домашні завдання
- 💬 Система підтримки

## ⚙️ Деплой на Render

### Швидкий старт:

1. **Створіть PostgreSQL базу** (обов'язково для продакшена):
   - Render Dashboard → New + → PostgreSQL
   - Name: `nz-diary-db`, Plan: `Free`
   - Скопіюйте **Internal Database URL**

2. **Створіть Web Service**:
   - Render Dashboard → New + → Web Service
   - Підключіть репозиторій `neverluckskr/NZUA-Diary-TelegramBot`
   - Build Command: `pip install -r requirements.txt`
   - Start Command: `python main.py`

3. **Додайте змінні окруження**:
   - `TELEGRAM_BOT_TOKEN` - токен від @BotFather
   - `OWNER_ID` - ваш Telegram User ID (дізнайтеся через @userinfobot)
   - `ADMIN_IDS` - список адмінів через кому (опціонально)
   - `DATABASE_URL` - автоматично додається з PostgreSQL (якщо використовуєте Blueprint)

**⚠️ Важливо:** На Free плані SQLite втрачається при кожному деплої. Для продакшена обов'язково використовуйте PostgreSQL!


## 👤 developer

telegram: @impulsedevfd