# NZ Diary Bot

Telegram бот для роботи з системою NZ.ua - отримання оцінок, розкладу, новин та іншої інформації.

## 🚀 Налаштування

### Вимоги

- Python 3.8+
- Telegram Bot Token (отримайте у [@BotFather](https://t.me/BotFather))

### Встановлення

1. Клонуйте репозиторій:
```bash
git clone <your-repo-url>
cd nzdiary
```

2. Створіть віртуальне середовище:
```bash
python -m venv venv
```

3. Активуйте віртуальне середовище:
- Windows:
```bash
venv\Scripts\activate
```
- Linux/Mac:
```bash
source venv/bin/activate
```

4. Встановіть залежності:
```bash
pip install -r requirements.txt
```

5. Налаштуйте змінні середовища:
```bash
# Windows (PowerShell)
$env:TELEGRAM_BOT_TOKEN="your_bot_token_here"

# Linux/Mac
export TELEGRAM_BOT_TOKEN="your_bot_token_here"
```

Або створіть файл `.env` на основі `env.example` (не комітьте `.env` в репозиторій!):
```bash
cp env.example .env
# Потім відредагуйте .env та вставте свій токен
```

6. Запустіть бота:
```bash
python main.py
```

## 📋 Функції

- 🔐 Авторизація в системі NZ.ua
- 📊 Перегляд оцінок та середнього балу
- 📅 Розклад уроків
- 📰 Новинна стрічка
- ⭐ VIP функції (автоматичні перевірки, експорт даних)
- 🎯 Нагадування про домашні завдання
- 💬 Система підтримки

## ⚙️ Конфігурація

У файлі `main.py` можна налаштувати:
- `OWNER_ID` - ID власника бота (для адмін-функцій) - **обов'язково змініть на свій ID!**
- `ADMINS` - список ID адміністраторів - **обов'язково змініть на свої ID!**
- `API_BASE` - базовий URL API (за замовчуванням: `https://api-mobile.nz.ua`)

## 🔒 Безпека

- Паролі та токени користувачів шифруються перед збереженням у базі даних
- Ключ шифрування зберігається у файлі `bot_encryption.key` (не комітьте його!)
- База даних `nz_bot.db` містить конфіденційні дані (не комітьте!)

## 📝 Залежності

Основні бібліотеки:
- `python-telegram-bot` - для роботи з Telegram API
- `cloudscraper` - для обходу захисту сайту
- `beautifulsoup4` - для парсингу HTML
- `cryptography` - для шифрування даних
- pip install python-telegram-bot cloudscraper beautifulsoup4 cryptography


## 👤 developer

telegram: @impulsedevfd