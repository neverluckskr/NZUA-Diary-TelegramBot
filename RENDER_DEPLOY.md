# 🚀 Деплой на Render

Пошаговая инструкция по деплою Telegram бота на Render.

## 📋 Подготовка

1. Убедитесь, что проект загружен на GitHub
2. Имейте готовый токен бота от @BotFather

## 🔧 Настройка на Render

### Вариант 1: Автоматическая настройка (через render.yaml)

1. Перейдите на [Render Dashboard](https://dashboard.render.com/)
2. Нажмите **"New +"** → **"Blueprint"**
3. Подключите ваш GitHub репозиторий
4. Render автоматически обнаружит `render.yaml` и настроит сервис

### Вариант 2: Ручная настройка

**⚠️ ВАЖНО:** На Free плане Render Background Worker недоступен. Используйте **Web Service** вместо этого.

1. Перейдите на [Render Dashboard](https://dashboard.render.com/)
2. Нажмите **"New +"** → **"Web Service"** (не Background Worker!)
3. Подключите GitHub репозиторий:
   - Выберите репозиторий `neverluckskr/NZUA-Diary-TelegramBot`
   - Выберите ветку `main`

4. **Настройки сервиса:**
   - **Name:** `nz-diary-bot` (или любое другое имя)
   - **Environment:** `Python 3`
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `python main.py`
   - **Plan:** `Free` (или выберите платный план)

5. **Переменные окружения (Environment Variables):**
   Нажмите **"Add Environment Variable"** и добавьте:
   ```
   TELEGRAM_BOT_TOKEN = ваш_токен_от_BotFather
   ```

6. Нажмите **"Create Web Service"**

**Примечание:** Хотя это Web Service, он будет работать как Background Worker для вашего бота, так как бот использует polling, а не веб-сервер.

## ⚙️ Важные настройки

### План (Plan)
- **Free план:** Подходит для тестирования, но сервис "засыпает" после 15 минут неактивности
- **Starter план ($7/мес):** Сервис работает постоянно, рекомендуется для продакшена

### Автоматические деплои
- По умолчанию включены автоматические деплои при каждом push в `main`
- Можно отключить в настройках сервиса

### Логи
- Логи доступны в реальном времени в разделе "Logs" вашего сервиса
- Полезно для отладки проблем

## 🔍 Проверка работы

1. После деплоя проверьте логи в Render Dashboard
2. Должны увидеть сообщения:
   ```
   [STARTUP] main() reached: checking BOT_TOKEN...
   [STARTUP] BOT_TOKEN present: True length=...
   [STARTUP] Application built
   ```
3. Попробуйте отправить команду `/start` боту в Telegram

## ⚠️ Важные замечания

### База данных SQLite
- На Render файловая система **эфемерная** (перезаписывается при каждом деплое)
- База данных `nz_bot.db` будет **теряться** при каждом деплое
- **Решение:** Используйте внешнюю базу данных (PostgreSQL на Render) или сохраняйте данные в облаке

### Ключ шифрования
- Файл `bot_encryption.key` также будет теряться
- При каждом деплое будет создаваться новый ключ
- **Решение:** Сохраняйте ключ в переменных окружения или используйте внешнее хранилище

### Рекомендации для продакшена
1. Используйте **PostgreSQL** вместо SQLite (Render предоставляет бесплатную БД)
2. Сохраняйте ключ шифрования в переменных окружения
3. Используйте **Starter план** для постоянной работы
4. Настройте мониторинг и алерты

## 🐛 Решение проблем

### Бот не отвечает
- Проверьте логи в Render Dashboard
- Убедитесь, что токен правильный
- Проверьте, что сервис запущен (не в состоянии "Sleep")

### Ошибки при деплое
- Проверьте, что `requirements.txt` содержит все зависимости
- Убедитесь, что Python версия совместима (Render использует Python 3.9+)
- Проверьте логи сборки (Build Logs)

### База данных не сохраняется
- Это нормально для SQLite на Render
- Перейдите на PostgreSQL или используйте внешнее хранилище

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи в Render Dashboard
2. Убедитесь, что все переменные окружения установлены
3. Проверьте документацию Render: https://render.com/docs

